# 手護視界 — 全專案更新完成報告

專案已更新到最新討論結果:上一輪的修復全部重新實作、第三代模組整合完成、
19 個檔案完成分類改名。**35 個檔案、10,717 行,兩套自我測試 119 項全數通過。**

---

## ⚠️ 必須先確認的一件事

專案知識庫裡的既有程式碼是**上一輪修改之前**的版本(沒有 IMU、沒有 `ir_mode.py`、
`hardware.py` 只有 49 行、`crosswalk.py` 的 PCA 還在、`ai_worker.py` 的
`yolo_paused` 整幀跳過還在)。

所以本輪這些檔案是**照《技術規劃 v3》第一節的規格重新實作**,不是還原你原本那份。
行為規格一致、測試通過,但實作細節必然與你手上的版本不同。

**特別注意 `percept_stairs_fusion.py`**:專案知識庫的索引裡有一份 v20(比 `/mnt/project`
掛載的 v19 新),表示你手上很可能有完整的 v20。我這份是照設計文件重建的,
合成測試數字對得上(k 誤差 0.4mm、階高誤差 0.1mm),但如果你的 v20 還在,
建議**只取我加的三段**:動態俯角 `set_pitch()`、手動/自動身高優先權、
回傳欄位補齊,其餘留你自己的。

---

## 一、本輪修好的東西

### 1. 一個會讓人站在樓梯前當機的契約錯誤(我自己造成的)

重寫 v20 時回傳 dict 少了 `has_depth` / `has_yolo`,而 `proc_ai_worker` 是用
`stairs_result["has_depth"]` 直接取值。`py_compile` 不會抓、import 測試不會抓、
`pyflakes` 也不會抓——只有在真的偵測到樓梯的那一刻才 KeyError,也就是使用者
正站在樓梯前面的時候。

已補齊全部 11 個 v19 欄位,v20 新欄位一律用「加的」;並在
`standalone_system_selftest.py` 第 1 組加了契約測試守著,包含模擬呼叫端
用 `[]` 取值的路徑。

### 2. 上一輪的修復,重新實作

| 項目 | 檔案 | 重點 |
|---|---|---|
| IMU 即時俯角 | `hw_camera.py` | `getConnectedIMU()` 驗 BNO086(rotation vector 只有這顆支援),四元數→俯角、EMA 平滑、`get_pitch()`;資料超過 1 秒未更新回 None——**IMU 掛掉時餵舊值比固定常數更危險** |
| 相機內參輸出 | `hw_camera.py` | `get_intrinsics()`,AprilTag 有內參才給得出位姿 |
| `yolo_paused` 整幀跳過 | `proc_ai_worker.py` | 改成只在 `infer_detect()` / 背景掃描內部讓出 GPU;深度型安全邏輯(避障、樓梯、落差)完全不受影響 |
| `SEARCH_REASON_VLM` 白工 | `proc_ai_worker.py` | 開關兩端行為一致,關閉時不再產生請求 |
| MQTT 靜默失效 | `hw_haptic.py` / `proc_main.py` | `on_disconnect`/`is_online()`/`offline_duration()`;離線超過 8 秒語音提醒(60 秒冷卻)。震動是唯一的即時安全管道,斷線時 publish 不報錯、訊息進虛空,使用者會誤以為前方沒障礙 |
| paho-mqtt 2.0 breaking change | `hw_haptic.py` | 先試 `CallbackAPIVersion.VERSION1`,`TypeError` 退回裸呼叫 |
| `MAX_JUMP_PX` 寫死 | `percept_trackers.py` | 改 `MAX_JUMP_RATIO`(畫面寬度比例),640 寬時等同舊的 200px |
| 斑馬線 PCA 不準 | `percept_crosswalk.py` | 移除 PCA,退階順序改 Hough → 質心連線。PCA 偏偏在「正對斑馬線」(矩形,兩特徵值接近)時主軸亂跳,而那正是最重要的情況 |
| 手動 vs 自動身高互搶 | `percept_stairs_fusion.py` | 手動 = 校驗基準,自動只做比對警告不覆蓋;差距大通常代表 `IMU_PITCH_OFFSET_DEG` 沒校準,靜默覆蓋會把校準錯誤變成看不見的系統性偏差 |
| IR 主動/被動切換 | `hw_ir_mode.py`(新) | 深度無效像素比例 + 雙門檻遲滯 + 連續確認 + 冷卻(三層防抖) |
| HTTPS 語音輸入 | `proc_main.py` / `mkcert_setup.sh`(新) | 雙埠並存:5000 HTTP 保底、5443 HTTPS 偵測到憑證才開 |
| 版本鎖定 | `requirements.txt`(新) | `depthai>=2.24.0,<3.0.0`、`paho-mqtt`、`numpy<2` |
| UI 物件搜尋 | `ui_v9.html` | 2×2 分頁點選 → **連續滑動掃描**;觸覺即時、語音 120ms 節流打斷式播報 |
| UI 滑動門檻 | `ui_v9.html` | 寫死 50px → 螢幕短邊 12% |
| UI 語音導覽 | `ui_v9.html` | 按住 2 秒念完整手勢說明 |

### 3. 第三代整合

`proc_main.py` 三行接上 `Gen3Pipeline`(AprilTag → SLAM → 分區記憶 → 觸覺信心編碼),
`proc_ai_worker.py` 回傳 `dets` 供偽VLM 場景投票,`proc_flask_app.py` 新增
`/api/height`、`/api/height/reset`、`/api/zone`。整包用 try 包住——**導盲的底線是避障,
定位是加值**,第三代壞掉不能影響主系統。

### 4. 檔名分類改名(19 檔)

由 `standalone_rename_migration.py` 執行:16 個檔案共 36 處 import 自動改寫、
全部 `py_compile` 通過、字串引用列出待人工確認(已改良比對規則,不再把
`search-overlay` 誤報成 `overlay`)。

啟動指令改為 **`python proc_main.py`**。

---

## 二、驗證結果

| 檢查 | 結果 |
|---|---|
| `standalone_system_selftest.py` | ✅ **51 項全過**(樓梯契約 9、v20 精度 5、身高優先權 6、IMU 俯角 5、手部門檻 4、斑馬線 4、IR 遲滯 6、MQTT 6、Flask API 8) |
| `standalone_gen3_selftest.py` | ✅ **68 項全過** |
| 樓梯 v20 合成測試 | ✅ k 誤差 0.4mm、σ 0.30mm、3/3 階、階高誤差 0.1mm |
| `py_compile` 全檔 | ✅ 32 個 Python 檔 |
| 實際 `import` | ✅ 21 個模組(另 8 個硬體相依模組以樁模組驗證通過) |
| `pyflakes` | ✅ 零警告(`legacy_stairs.py` 為已停用檔案,不動) |
| UI JavaScript 語法 | ✅ `node --check` 通過(413 行) |
| 舊模組名殘留 | ✅ import 陳述式零殘留 |

---

## 三、全系統行數

| 分類 | 檔數 | 總行 | 程式 | 註解/文件 | 空行 |
|---|---:|---:|---:|---:|---:|
| `hw_` 硬體層 | 6 | 1,167 | 736 | 291 | 140 |
| `percept_` 感知層 | 8 | 2,346 | 1,401 | 625 | 320 |
| `memory_` 狀態層 | 2 | 724 | 446 | 176 | 102 |
| `proc_` 流程層 | 7 | 3,113 | 2,296 | 433 | 384 |
| `shared_` 共用 | 3 | 916 | 368 | 420 | 128 |
| `standalone_` 獨立工具 | 5 | 1,086 | 792 | 156 | 138 |
| `legacy_` 已停用 | 1 | 698 | 433 | 166 | 99 |
| **Python 小計** | **32** | **10,050** | **6,472** | **2,267** | **1,311** |
| `ui_v9.html` | 1 | 581 | | | |
| `requirements.txt` | 1 | 26 | | | |
| `mkcert_setup.sh` | 1 | 60 | | | |
| **系統總計** | **35** | **10,717** | | | |

最大三個檔:`proc_ai_worker.py` 1,268、`legacy_stairs.py` 698、`proc_vlm_worker.py` 531。
註解/文件佔 Python 程式碼的 22.6%。

---

## 四、實機部署順序

1. **`IMU_PITCH_OFFSET_DEG` 實測填入**(`shared_config.py`)。俯角的軸向與正負號
   也必須實機驗證——裝設方向不同可能要改成 `-asin` 或換軸,`hw_camera._quat_to_pitch_deg`
   的註解有標。這是所有幾何運算的基準,沒校準等於前面全部白做。
2. `pip install -r requirements.txt`(含 `pupil-apriltags`,有它才有真的 `decision_margin`)
3. `./mkcert_setup.sh <Jetson固定IP>`,手機安裝根 CA → 語音輸入才能用
4. 列印 tag36h11(12–15cm)與 QR,`python proc_setup_wizard.py` 走一次建置流程
5. `python proc_main.py`
6. 現場校準:IR 門檻(`IR_INVALID_HIGH/LOW`)、樓梯階高門檻、UI 語音節流 120ms
   ——這幾個都標了「未經實機驗證」

---

## 五、仍未做的事

| 項目 | 卡在哪 |
|---|---|
| ORB-SLAM3 實際綁定 | 等主板拍板;`percept_slam.py` 第一版是特徵健康度監測,不是 SLAM,介面已留好 |
| §4.6 位移降級的訊號源 | 需要 SLAM 位移或 IMU 步數,第一版只有時間降級 |
| 2×7 十四顆馬達 | 信心震動目前借用 7 馬達陣列的正前那顆 |
| `X_LINK_ERROR` 根因 | 從未驗證(懷疑 USB 頻寬/供電) |
| Traffic 夜間可靠性 | 從未實測,安全風險最高 |
| 三合一投票融合 | `object_memory` / `PrescanTagCache` / SLAM 座標統一,設計方向已定 |
